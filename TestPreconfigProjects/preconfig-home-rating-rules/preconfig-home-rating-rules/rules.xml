<project>
    <!-- Project name. -->
    <name>preconfig-home-rating-rules</name>

    <!-- OpenL project includes one or more modules.  -->
    <modules>
        <module>
            <name>datatypes</name>
            <rules-root path="rules/datatypes.xlsx"/>
        </module>
        <module>
            <name>HOME</name>
            <rules-root path="rules/HOME-*.xlsx"/>
        </module>
    </modules>

    <!-- Project's classpath. -->
    <classpath>
        <entry path="lib/*.jar"/>
    </classpath>
</project>
