<project>
    <!-- Project name. -->
    <name>preconfig-umbrella-rating-rules</name>

    <!-- OpenL project includes one or more modules.  -->
    <modules>
        <module>
            <name>datatypes</name>
            <rules-root path="rules/datatypes.xlsx"/>
        </module>
        <module>
            <name>UMBRELLA</name>
            <rules-root path="rules/UMBRELLA-*.xlsx"/>
        </module>
    </modules>

    <!-- Project's classpath. -->
    <classpath>
        <entry path="lib/*.jar"/>
    </classpath>
</project>
